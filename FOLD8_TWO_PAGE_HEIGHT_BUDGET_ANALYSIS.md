# تحليل هندسي لميزانية الارتفاع — Fold 8 ووضع الصفحتين المتقابلتين

> **نطاق الوثيقة:** تحليل وتشخيص واقتراح هندسي فقط. لا تتضمن هذه الوثيقة أي تعديل برمجي أو تغيير في الخطوط أو بيانات المصحف أو Android أو Capacitor.
>
> **مصدر القياسات الأساسية:** تقرير WebView المباشر المرسل لبيئة المحاكي في وضع الشاشة الداخلية المفتوحة، بتاريخ 13 أغسطس 2026.

---

## 1. النتيجة التنفيذية

سبب ظهور صفحة واحدة على بيئة Fold 8 العريضة ليس عرض الشاشة، ولا QCF، ولا فشل تحميل الصفحة الثانية، ولا اعتماد التطبيق على نقطة عرض ثابتة.

السبب الهندسي هو أن التطبيق يرى مساحة CSS فعلية قدرها:

```text
816 × 616
```

ثم يحجز من ارتفاعها ميزانية ثابتة قدرها:

```text
112px أعلى + 96px أسفل = 208px
```

فتصبح أعلى مساحة متاحة لمسرح المصحف:

```text
752 × 408
```

وبالتالي يكون المقياس النهائي للانتشار:

```text
S_final = 14.703px
```

بينما الحد الأدنى الفعلي المطبق في المخطط هو:

```text
18px
```

لذلك ينتهي القرار الهندسي إلى:

```text
Single Page
reason = insufficient-space
```

لا يوجد شرط فعلي باسم `720px` أو نسبة `1.25` داخل كود التطبيق الحالي. هاتان القيمتان في تقرير المحاكي ليستا قرار مخطط الانتشار الفعلي.

---

## 2. التمييز بين البكسلات الفيزيائية وبكسلات CSS

### القيم الواردة من WebView

| القياس | القيمة |
|---|---:|
| `window.innerWidth` | `816px` |
| `window.innerHeight` | `616px` |
| `visualViewport.width` | `816px` |
| `visualViewport.height` | `616px` |
| `devicePixelRatio` | `3` |
| الدقة الفيزيائية | `2448 × 1848` |

هذه القيم متسقة:

```text
2448 ÷ 3 = 816
1848 ÷ 3 = 616
```

إذن الدقة الفيزيائية الكبيرة لا تعني أن مخطط CSS يحصل على `2448px` عرضًا. التطبيق يحسب هندسة الصفحات ببكسلات CSS، وبالتالي مصدر الحقيقة هنا هو:

```text
816 × 616
```

---

## 3. خريطة الحاويات من الشاشة إلى مسرح الصفحات

```text
WebView viewport
816 × 616
↓
QuranReaderScreen root
fixed / h-[100vh] / px-4 sm:px-8
↓
Content Area
flex-1 / max-w-5xl / mx-auto
↓
.qcf-page-viewport
pt-24 pb-24 sm:pt-28 sm:pb-24
↓
.mushaf-spread-measure
المنطقة التي يقيسها مخطط الانتشار فعليًا
↓
MushafSpreadPlanner
↓
Single slot أو Right + Left slots
```

### الملفات المسؤولة

| الطبقة | الملف | المسؤولية |
|---|---|---|
| غلاف القارئ | `src/components/QuranReaderScreen.tsx` | الحواف، منطقة المحتوى، عناصر التحكم، الحجز العلوي والسفلي |
| قياس الحاوية | `src/hooks/useMushafSpreadLayout.ts` | `ResizeObserver` و`visualViewport` وقطاعات العرض |
| القرار الهندسي | `src/services/MushafSpreadPlanner.ts` | حساب `S` واختيار `single` أو `spread` |
| الرسم | `src/components/MushafSpreadSurface.tsx` | رسم مواضع الصفحات التي يقررها المخطط |
| تنسيق المسرح | `src/index.css` | `.mushaf-spread-measure` ومواضع الصفحات المطلقة |

---

## 4. تفصيل ميزانية العرض الحالية

### 4.1 الحواف الأفقية

في الجذر توجد القاعدة:

```text
px-4 sm:px-8
```

عند عرض `816px` تكون قاعدة `sm` فعالة، لذا يصبح الحشو:

```text
32px يمين + 32px يسار = 64px
```

الحاوية التالية تحمل:

```text
max-w-5xl
```

والحد الأقصى لها `1024px`. هذا الحد **غير فعال** على Fold 8، لأن العرض بعد الحواف هو فقط:

```text
816 - 64 = 752px
```

إذن:

```text
عرض منطقة المحتوى = 752px
```

### 4.2 الحجز الرأسي الصريح

منطقة الصفحات تستخدم:

```text
pt-24 pb-24 sm:pt-28 sm:pb-24
```

وعند عرض `816px` تصبح القيم الفعلية:

| الحجز | الصنف | القيمة |
|---|---|---:|
| أعلى | `sm:pt-28` | `112px` |
| أسفل | `sm:pb-24` | `96px` |
| الإجمالي | — | `208px` |

هذه ليست نتيجة جمع ارتفاع العناصر تلقائيًا. إنها **ميزانية ثابتة صريحة** لحماية منطقة المصحف من عناصر التحكم المطلقة.

### 4.3 أعلى مساحة ممكنة لمسرح الصفحات

بافتراض أن منطقة المحتوى تأخذ ارتفاع WebView الكامل، وهو ما يطابق بنية `flex-1` الحالية مع عنصر الصوت المخفي:

```text
ارتفاع المحتوى = 616px
الحجز الرأسي = 208px
ارتفاع مسرح الصفحات = 408px
```

إذن:

```text
.mushaf-spread-measure ≈ 752 × 408
```

هذه قيمة مشتقة من قياس WebView الفعلي والقواعد الفعلية في الكود. القياس المباشر للعنصر من DevTools يظل اختبار التأكيد النهائي، لكنه لا يستطيع أن يزيد هذه المساحة فوق هذا الحد من دون تغير بنيوي في الحجز.

---

## 5. العناصر التي تبرر الحجز العلوي

كل العناصر التالية `absolute` أو `fixed`، ولذلك لا تحجز مساحة Flex تلقائيًا. الحجز `112px` هو الحماية اليدوية التي وضعها القارئ لها.

| العنصر | الموضع المعلن | القياس المعلن | هل هو ثابت؟ | ملاحظات |
|---|---|---|---|---|
| زر الخروج | `top-6 right-6` | `40 × 40px` | يظهر عند `showControls` | يمتد تقريبًا من `y=24` إلى `y=64` |
| زر حفظ الصفحة | `top-6 left-6` | `40 × 40px` | يظهر عند `showControls` | يماثل زر الخروج |
| معلومات الجزء والسورة | `top-6` | حجم خط `10px` أو `12px` | موجودة دائمًا | لا ارتفاع صريح؛ ارتفاعها تابع للخط |
| كبسولة التقدم | `top-12` | `py-1.5` = `6px` أعلى وأسفل | تظهر بعد تحميل الصفحة | النص `10px`، والارتفاع النهائي تابع للخط والحدود |

### الاستنتاج العلوي

أعمق عنصر علوي ثابت معلوم الموضع هو كبسولة التقدم التي تبدأ عند:

```text
48px
```

وزر الخروج ينتهي عند:

```text
64px
```

الحجز الحالي يبدأ مسرح الصفحات عند:

```text
112px
```

أي أنه يقدم هامش أمان علويًا كبيرًا نسبيًا. هذا الهامش ضروري جزئيًا للظلال والحركة، لكنه أكبر من ارتفاع العناصر المعلنة نفسها.

---

## 6. العناصر التي تبرر الحجز السفلي

| العنصر | الموضع المعلن | القياس المعلن | هل هو ثابت؟ | ملاحظات |
|---|---|---|---|---|
| رقم الصفحة | `bottom-6 left-8` | خط `12px` أو `14px` | دائم | يقع عادة في هامش الصفحة الأيسر، لا وسط النص |
| بطاقة التحكم الأساسية | `bottom-8` | أكبر زر داخلي `36px` + `16px` حشو رأسي + حدود | تظهر عند `showControls` | ارتفاع اسمي يقارب `54px` |
| ورقة مشغل التكرار | `bottom-8` | أكبر زر داخلي `48px` + `16px` حشو رأسي + حدود | فقط عند `isPlaying && playMode === 'single'` | ارتفاع اسمي يقارب `66px` |
| بطاقة إجراء الآية | `bottom: 5.5rem` | لا ارتفاع صريح | تظهر بعد الضغط المطول | مؤقتة، لكنها قد تتقاطع مع مساحة القراءة إن لم تكن لها سياسة خاصة |
| نافذة الإعدادات الصوتية أو السمة | `bottom-24` | عرض `288px` وحشو `20px` | مؤقتة | Modal، لا ينبغي أن تحدد ميزانية القراءة الدائمة |

### الاستنتاج السفلي

`pb-24 = 96px` يمثل احتياطيًا ثابتًا يغطي بطاقة التحكم السفلية، وحالة المشغل، والظلال، ومسافة أمان إضافية.

لكنه لا يتغير عندما:

```text
showControls = false
isPlaying = false
playMode ≠ single
```

إذن هذا الحجز **ثابت** رغم أن بعض العناصر التي يحميها ديناميكية.

---

## 7. ما ليس سببًا في فقد الارتفاع

| العامل | النتيجة |
|---|---|
| `max-w-5xl` | يحد العرض فقط، وليس الارتفاع؛ وغير فعال على عرض `752px` |
| `mushaf-spread-measure` | لا حشو ولا هامش ولا gap؛ يأخذ مساحة Flex المتبقية فقط |
| `.mushaf-spread-surface` | `absolute; inset: 0`، لا يستهلك ارتفاعًا إضافيًا |
| `.mushaf-page-slot` | موضع مطلق من المخطط، لا يضيف حجزًا |
| QCF page | أبعادها تتبع `S` ولا تفرض ارتفاعًا أعلى من الخطة |
| Android safe-area | لا توجد `env(safe-area-inset-*)` في مسار القارئ |
| Android system bars | مخفية عبر Immersive Mode؛ ليست مصدر `112px` أو `96px` |
| transform أو scale | لا يوجد transform أو scale على مسرح الصفحات يفسر الوضع المفرد |

---

## 8. الحساب الهندسي الحالي على Fold 8

ثوابت المخطط الفعلية:

```text
عرض الصفحة المرجعي = 17
ارتفاع الصفحة المرجعي = 27.75
الفاصل المرجعي = 0.8
الحد الأدنى للمقياس المزدوج = 18px
```

### قيد العرض

```text
S_width = 752 ÷ (17 + 17 + 0.8)
S_width = 752 ÷ 34.8
S_width = 21.609px
```

### قيد الارتفاع

```text
S_height = 408 ÷ 27.75
S_height = 14.703px
```

### القرار

```text
S_final = min(21.609, 14.703)
S_final = 14.703px
```

```text
14.703px < 18px
```

لذلك المخطط يختار:

```text
mode = single
reason = insufficient-space
```

### ما يلزم لتفعيل الانتشار عند المقياس الحالي الآمن

```text
الارتفاع المطلوب = 18 × 27.75 = 499.5px
الارتفاع المتاح = 408px
العجز = 91.5px
```

```text
العرض المطلوب = 18 × 34.8 = 626.4px
العرض المتاح = 752px
فائض العرض = 125.6px
```

النتيجة الحاسمة:

```text
العرض كافٍ.
الارتفاع غير كافٍ.
```

---

## 9. مقارنة مع بيئة ينجح فيها الانتشار

بيئة اختبار ناجحة سابقة:

```text
WebView / Browser viewport = 1024 × 768
مسرح الصفحات = 960 × 560
```

| البند | Fold 8 | البيئة الناجحة |
|---|---:|---:|
| عرض WebView | 816 | 1024 |
| ارتفاع WebView | 616 | 768 |
| عرض مسرح الصفحات | 752 | 960 |
| ارتفاع مسرح الصفحات | 408 | 560 |
| `S_width` | 21.609 | 27.586 |
| `S_height` | 14.703 | 20.180 |
| `S_final` | 14.703 | 20.180 |
| الحد المطلوب | 18 | 18 |
| النتيجة | مفرد | انتشار |

العامل الذي يفسر الفرق:

```text
560 - 408 = 152px
```

منطقة العرض الناجحة تملك ارتفاعًا إضافيًا قدره `152px` مقارنة ببيئة Fold 8 الحالية.

---

## 10. هل Foldable Detection مسؤول؟

### ما يفحصه التطبيق فعلًا

```text
window.viewport?.segments
ثم
window.visualViewport?.segments
```

ويعيد القياس عند:

```text
ResizeObserver
window.resize
visualViewport.resize
visualViewport.scroll
screen.orientation.change
```

### ما لا يستخدمه التطبيق

```text
CSS env(viewport-segment-*)
CSS horizontal-viewport-segments
CSS vertical-viewport-segments
Android WindowManager
FoldingFeature
Capacitor Native Bridge
```

### الحكم

غياب معلومات المفصل ليس مطلوبًا لتفسير النتيجة الحالية، لأن الحساب المستمر العادي يفشل أصلًا بسبب الارتفاع.

حتى لو كشفت المنصة قطاعين جانبيين، فإن كل قطاع يظل مقيدًا بالارتفاع المتاح نفسه تقريبًا، وبالتالي ينخفض المقياس تحت `18px` أيضًا.

لا يوجد دليل هندسي حالي يبرر Android Bridge لحل هذه الحالة بالذات.

---

## 11. البدائل الهندسية لدعم Fold 8 لاحقًا

### البديل A — خفض الحد الأدنى للمقياس

| البند | النتيجة |
|---|---|
| الحالة | جعل الحد `≤ 14.703px` |
| مساحة مستعادة | `0px` |
| النتيجة | تظهر صفحتان أصغر |
| الخطر | يخالف حد القراءة الآمن المعتمد ويقلص المصحف بنحو 18٪ |
| القرار | **مرفوض** |

### البديل B — إخفاء عناصر التحكم تلقائيًا

| البند | النتيجة |
|---|---|
| الحالة | إخفاء عناصر التحكم واستعمال كامل الارتفاع |
| مسرح الصفحات المحتمل | حتى `752 × 616` |
| `S_height` | `22.198px` |
| المشكلة | عند ظهور العناصر تتغير هندسة الصفحة أو تتراكب عليها |
| القرار | غير آمن كحل أساسي |

### البديل C — تخفيض الحجز العلوي والسفلي فقط

مثال محافظ:

```text
أعلى = 88px
أسفل = 24px
الإجمالي = 112px
```

| البند | القيمة |
|---|---:|
| الارتفاع المستعاد | `96px` |
| ارتفاع المسرح الجديد | `616 - 112 = 504px` |
| `S_height` | `504 ÷ 27.75 = 18.162px` |
| النتيجة | يمر الحد `18px` بفائض `0.162px` فقط |

هذا ينجح حسابيًا، لكنه يملك هامش أمان صغيرًا جدًا قدره `4.5px` فقط في الارتفاع.

**القرار:** ممكن، لكنه ليس البديل الأكثر راحة بسبب الهامش الضيق.

### البديل D — تخطيط Spread خاص مع شريط تحكم جانبي

الاقتراح:

```text
أعلى = 88px
أسفل = 24px
بطاقة التحكم السفلية الدائمة → شريط جانبي رأسي
```

| البند | القيمة |
|---|---:|
| الارتفاع المستعاد | `96px` |
| ارتفاع مسرح الصفحات | `504px` |
| `S_final` | `18.162px` |
| المقياس الآمن | محفوظ |
| القص | غير مطلوب |
| تعديل QCF أو البيانات | غير مطلوب |

عند هذا المقياس:

```text
عرض الصفحة الواحدة ≈ 308.76px
فاصل الصفحتين ≈ 14.53px
العرض الكلي للانتشار ≈ 632.04px
```

المساحة الأفقية الحرة:

```text
752 - 632.04 = 119.96px
≈ 60px في كل جانب
```

هذا يكفي لوضع شريط تحكم رأسي بعرض عملي يقارب:

```text
48px
```

مع هامش بسيط، خارج مستطيلات الصفحتين.

### البديل E — تخطيط Spread أكثر أمانًا بهامش إضافي

الاقتراح:

```text
أعلى = 80px
أسفل = 24px
الإجمالي = 104px
```

| البند | القيمة |
|---|---:|
| الارتفاع المستعاد | `104px` |
| ارتفاع مسرح الصفحات | `512px` |
| `S_height` | `18.450px` |
| فائض المقياس عن الحد | `0.450px` |

هذا يوفر هامشًا أفضل من البديل D، لكن نجاحه يتوقف على قياس بصري فعلي لتأكد أن كبسولة التقدم وظلالها لا تتجاوز `80px` من أعلى.

### البديل F — نقل جميع التحكمات إلى الجانبين

| البند | النتيجة |
|---|---|
| الحجز الرأسي | قريب من الصفر |
| `S_height` | حتى `22.198px` |
| الميزة | أكبر مساحة ممكنة للمصحف |
| الخطر | تغيير بصري وسلوكي كبير عن الهوية الحالية |
| القرار | غير مطلوب حاليًا؛ تدخل أكبر من الحاجة |

---

## 12. التخطيط الموصى به

# البديل الموصى به: شريط تحكم جانبي خاص بالانتشار مع ميزانية رأسية مستقرة

التوصية الهندسية:

```text
Spread mode فقط:

احتياطي علوي محافظ ≈ 88px
احتياطي سفلي صغير ≈ 24px
بطاقة التحكم الدائمة السفلية → شريط جانبي رأسي
```

### لماذا هذا هو الأكثر أمانًا؟

1. يحافظ على مقياس `18px` ولا يضغط الصفحات.
2. يستعيد `96px`، أي أكثر من العجز المطلوب `91.5px`.
3. يستفيد من فائض العرض الحقيقي الموجود على Fold 8 (`≈120px`).
4. لا يغير أبعاد صفحات المصحف أو QCF أو بياناتها.
5. لا يحتاج Android Bridge أو معرفة اسم الجهاز.
6. لا يغير وضع الصفحة المفردة إطلاقًا.
7. لا يفرض ظهور أو اختفاء عناصر التحكم ليتغير حجم الصفحات.
8. يبقي هندسة الصفحة مستقرة أثناء إظهار وإخفاء التحكمات.

### شرط مهم قبل التنفيذ

البديل D ينجح بفائض ارتفاع صغير جدًا:

```text
504px available
499.5px required
4.5px safety margin
```

لذلك لا يجوز تنفيذه قبل قياس مباشر لهذه العناصر على Fold 8:

```text
ارتفاع كبسولة التقدم النهائي مع الظل
ارتفاع بطاقة التحكم السفلية النهائي
ارتفاع ورقة المشغل عند وضع التكرار المفرد
حدود بطاقة إجراء الآية
موضع رقم الصفحة النهائي
```

إذا أثبت القياس أن `88px` غير كافية أعلى الصفحة، فالبديل E هو الاحتياطي التالي، مع نقل ما يلزم من عناصر التحكم إلى الجانب أيضًا.

---

## 13. سياسة التحكمات في Spread mode المقترحة لاحقًا

### عناصر تبقى أعلى الشاشة

```text
زر الخروج
زر الحفظ
معلومات السورة والجزء
كبسولة التقدم المختصرة
```

### عناصر تنتقل إلى الشريط الجانبي

```text
التشغيل أو الإيقاف
اختيار القارئ
إعدادات المظهر
إعدادات التكرار عند الحاجة
```

### عناصر مؤقتة يجب ألا تتراكب على صفحات الانتشار

```text
بطاقة إجراء الآية
ورقة المشغل المفرد
إعدادات الصوت
```

السياسة المناسبة لها لاحقًا:

```text
إما استخدام الشريط الجانبي نفسه
أو نافذة Modal واضحة فوق طبقة معتمة
أو إعادة القارئ مؤقتًا إلى صفحة مركزة قبل إظهار عنصر كبير
```

ولا يجوز أن تظهر كبطاقة شفافة فوق آخر سطور الصفحة في وضع الانتشار.

---

## 14. ما يجب عدم لمسه

لا علاقة للحل المقترح بهذه الطبقات، ويجب أن تبقى كما هي:

```text
QCF_P001 … QCF_P604
qpcV2
MushafQcfV2LayoutService
QcfFontStorage
useQcfFont
QuranOfflineService
IndexedDB
Directory.Data
Filesystem.readFile
فك الضغط
Capacitor
Android
بيانات المصحف
بيانات الصفحات
محاذاة الأسطر
```

---

## 15. القياسات المباشرة المطلوبة قبل اعتماد تنفيذ بديل D أو E

لرفع الثقة إلى 100٪ قبل أي تعديل، يلزم من Fold 8 نفسه تسجيل:

```text
.mushaf-spread-measure.clientWidth
.mushaf-spread-measure.clientHeight
.qcf-page-viewport computed padding-top / padding-bottom
.mushaf-spread-surface data-mushaf-display-mode
window.viewport?.segments
window.visualViewport?.segments
visualViewport.scale
```

إذا كانت النتيجة المتوقعة:

```text
stage ≈ 752 × 408
mode = single
segments = [] أو segment واحد
```

فإن سبب المشكلة يصبح مثبتًا نهائيًا:

```text
Height-constrained minimum-scale failure
```

---

## 16. القرار النهائي لهذه المرحلة

لا يلزم الآن:

```text
Native Bridge
Android WindowManager
إعادة بناء قارئ المصحف
تغيير الخطوط
تغيير QCF
خفض الحد الآمن للمقياس
```

المشكلة تقع داخل:

```text
ميزانية الارتفاع الخاصة بغلاف القارئ في Spread mode
```

والاتجاه الهندسي الأكثر أمانًا لاحقًا هو:

```text
Spread-specific control layout
+ side control rail
+ top/bottom budget محسوب
+ الحفاظ على S ≥ 18
+ عدم تغيير Single Page
```
